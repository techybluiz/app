import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  topicItem: {
    alignItems: "center",
  },
  topicIcon: {
    width: 60,
    height: 60,
    borderRadius: 30,
    justifyContent: "center",
    alignItems: "center",
    marginBottom: 8,
  },
  topicEmoji: {
    fontSize: 24,
  },
  topicLabel: {
    fontSize: 12,
    color: "#666",
    textAlign: "center",
  },
})
